function matrix(num){
    
    for (let index = 0; index < num; index++) {
        let result = "";
        for (let index = 0; index < num; index++) {
            result += `${num} `;
        }
        console.log(result);
    }
}